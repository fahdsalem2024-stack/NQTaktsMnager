{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python fix_db.py && gunicorn --bind 0.0.0.0:8080 app:app",
        "healthcheckPath": "/",
        "restartPolicyType": "ON_FAILURE"
    }
}